version: '2'

services:

    postgres1:
        image: postgres
        restart: always
        ports:
            - 15432:5432
        environment:
            POSTGRES_USER: postgres
            POSTGRES_PASSWORD: postgres

    postgres:
        image: postgres
        restart: always
        ports:
            - 25432:5432
        environment:
            POSTGRES_USER: postgres
            POSTGRES_PASSWORD: postgres
